#include "main.h"
#include <string.h>

/**
 * main - code creates function returns the length of a string.
 * s : is the input
 * Return: 0 means code is sucessful
 */

int _strlen(char *s)
{

int string_length = 0;

while (*s ! = 0)
{
string_length++;
s++;
}

return(length);
}
